# --- API server ---
PORT=3000

# CORS is open (all origins) in server.js — no CORS_* vars needed.

# --- Gmail (contact demo) ---
CONTACT_GMAIL_USER=
CONTACT_GMAIL_APP_PASSWORD=
# CONTACT_MAIL_FROM_NAME=SiteMind AI

# --- OpenAI (scrape structuring + chat test) ---
# OPENAI_API_KEY=
