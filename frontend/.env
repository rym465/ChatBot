# Local: leave unset — Vite proxies /api → http://127.0.0.1:3000 (see vite.config.js).
# Production build (e.g. Vercel): set to your deployed Node API origin, no trailing slash.
VITE_API_BASE=https://white-label-ai-chatbot-generator-ty.vercel.app
