# Deployed landing (Vercel) — example only; do NOT use this as VITE_API_BASE.
# https://white-label-ai-chatbot-generator-ty.vercel.app

# --- Browser → API (optional) ---
# If src/api.js → API_BASE_URL is empty, you can set at build time:
# VITE_API_BASE=https://your-node-api.example.com

# --- Vercel dashboard only (serverless proxy: frontend/api/proxy.js) ---
# Same-origin /api/* on Vercel is rewritten to this backend. No trailing slash.
# BACKEND_URL=https://your-node-api.onrender.com
